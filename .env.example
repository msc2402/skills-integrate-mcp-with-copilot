# Database Configuration for Mergington High School Activities System

# Development (Default)
# Uses SQLite with WAL mode for better concurrency
DATABASE_URL=sqlite+aiosqlite:///./mergington_activities.db

# Testing
# Uses in-memory SQLite for fast tests
# DATABASE_URL=sqlite+aiosqlite:///:memory:

# Production (PostgreSQL example)
# Uncomment and configure for production deployment
# DATABASE_URL=postgresql+asyncpg://username:password@localhost/mergington_activities

# Debug Mode
# Set to true to enable SQL query logging
DEBUG=false

# Backup Settings
# Directory to store database backups
BACKUP_DIR=./backups

# Connection Pool Settings (for production databases)
# DB_POOL_SIZE=20
# DB_MAX_OVERFLOW=0
# DB_POOL_TIMEOUT=30
# DB_POOL_RECYCLE=3600
